{
  "name": "dynopay-frontend-bridge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd /app && NODE_OPTIONS=--max-old-space-size=2048 HOSTNAME=0.0.0.0 PORT=3000 node_modules/.bin/next dev -p 3000 -H 0.0.0.0",
    "build": "cd /app && node_modules/.bin/next build",
    "dev": "cd /app && node_modules/.bin/next dev -p 3000 -H 0.0.0.0"
  }
}

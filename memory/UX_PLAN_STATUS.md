# Premium in-app experience — plan status & remaining work

Last updated: 2026-09-10 (session 5: **close-out — 1.2 / 1.12 / 3.3 / 3.6 / 3.8 verified and DONE; sidebar setup-progress ring built; add-wallet OTP marker added; `/ux-plan/status.html` now generated from this file by `scripts/ux_plan_status_html.py`**). Also shipped from the product backlog: a live **Gateway health strip** on /dashboard (`GET /api/status/gateway`). Source of truth for the approved 4-checkpoint plan ("Premium, intuitive in-app experience: sign-up → every merchant page, on every device"). **Every row of the 4-checkpoint plan is now DONE or verified PRESENT.** The final frontend testing_agent pass (4.2 / 4.4 / 3.2 + this session's additions) is deferred by the user.

Shareable rendered copy: `/ux-plan/status.html` on the preview URL (regenerate: `python3 scripts/ux_plan_status_html.py`).

## Legend

- **DONE** — delivered and verified in this engagement
- **PARTIAL** — started; specific pieces remain (listed)
- **PRESENT** — already exists in the app (found in audit); needs verification/alignment to the quality bar, not a rebuild
- **PENDING** — not started

## Headline

- **Plan complete.** All 4 checkpoints delivered: foundations + shell + onboarding (tokens, tips on 14 pages, copy ticks, one status chip, unified auth, guided first-run wizard + new-merchant hero + sidebar progress ring), core money pages (link detail drawer, two-pane create, products grid/panel, transactions bottom-sheet filters, coin-filtered balances), remaining pages (wallet security, KYC, settings unsaved-state, notifications inbox, receipts drawer, referrals, developers verified), and the cross-device sweep (responsive 160/160, motion, empty states, 6 languages, keyboard, fresh-account walkthrough).
- Rows marked PRESENT were audited against the quality bar and needed no rebuild (page header, metric card, data lists, detail panels, empty states, forms, toasts, header, bottom bar, tablet rail, transitions, storefront, customers, help).
- Verification: testing_agent iterations 138–144 + scripted sweeps (`scripts/qa/*`). Deferred by the user: one last frontend testing_agent smoke pass over the final session's additions before Save to GitHub.

---

## Checkpoint 1 — Foundations, shell & onboarding

| # | Item (from plan) | Status | Remaining / notes |
|---|---|---|---|
| 1.1 | Visual tokens consolidated once (8-pt spacing, 16px radius, type scale, motion 150–250 ms, reduced-motion) | DONE (2026-09-09) | `styles/uxTokens.ts` is the single documented entry point: re-exports `CB_TOKENS`, `MONO`, `Icon`, `MonoAmount`; names `SPACE` (8-pt), `RADIUS` (chip 999 · control 12 · card 16 · sheet 20 · phone 30), `TYPE` scale, `MOTION` (150/200/250 ms + shared ease-out), `transition()`, `motionSafe()`, `REDUCED_MOTION`, `usePrefersReducedMotion()`, `themeInk()`. No visual change; new UI imports from here. |
| 1.2 | First-visit tip — dismissible, one-time, per page | DONE (2026-09-10) | Live on the **7 core pages** (Payment links, Transactions, Balances, Payout wallets, Receipts & Tax, Customers, Refer & earn) **+ 5 secondary pages** (Your page `/storefront`, Settings, Developers, Notifications, Help & Support) **+ Wallet security (`/wallet/security`) and KYC (`/kyc`)** — the last two landed with 3.4 / 3.10 (`pageTips.walletSecurity`, `pageTips.kyc`). Route-keyed from `ClientLayout`; copy in EN/PT/FR/ES/DE/NL (`scripts/apply_secondary_page_tips.py`). |
| 1.3 | Copy fields with a confirm tick | DONE | Shared `CopyInline` (now with a `boxed` variant for hash/key rows) adopted on: dashboard referral code, payout-wallet address, **API keys** (secret key card + publishable keys, incl. just-created card), **transaction detail drawer** (settled-to address, incoming/outgoing hash, callback URL — success toast removed, toast now failure-only), **payment-link success modal** (link + wallet address), **quick-create panel**, **storefront Share tab** (inline icon next to the URL; the QR card's own "Copy link" button already flips to "Copied!" ✓). The **mobile referral banner** listed in the audit was already retired from the mobile dashboard (commit 1100656ad — referral lives in the rail card + Referrals page); its dead component file was deleted. Snippet "Copy" text buttons on Developers intentionally keep their toast (not a field). Testids: `data-copied="true"` while the tick shows. |
| 1.4 | Status chip — the 7 statuses, one look, same colour + dot everywhere | DONE | Canonical `Components/UI/StatusChip` + `helpers/txStatus` (`toTxStatusBucket`, `TX_STATUS_TONE`): Settled ● emerald · Confirmed ● indigo · Processing / Pending ● amber · Awaiting ○ hollow amber · Unpaid ○ hollow slate · Failed ● rose; labels from the `transactions` namespace (6 langs); tooltip per state. Adopted on dashboard recent transactions (was collapsing Confirmed → "Paid" in green), Transactions table + toolbar + detail drawer, Customers payment history, Notifications → detail drawer. `TransactionStatusBadge` is now an alias. Testids: chips default to `tx-status-<bucket>`; the Transactions table row (`tx-row-status`) and drawer pill (`tx-modal-status`) keep their existing ids — all expose `data-status=<bucket>`. Payment-link status label aligned (desktop "Completed" → "Paid", same as phone). Non-transaction chips (link Active/Expired, product Live/Draft, customer segments) keep `StatusDot` with the same colour map. |
| 1.5 | Page header primitive (title, purpose line, primary action, breadcrumb) | PRESENT | Shared `MainPageHeader` in `ClientLayout` used by most pages. Verify: breadcrumb on sub-pages; primary action always in the same place. |
| 1.6 | Metric card (value, label, change, sparkline) | PRESENT | Dashboard KPI cards exist. Verify identical reuse inside pages. |
| 1.7 | Data list — table on large screens, card list on phones; same sort/filter/pagination/skeleton/empty | PRESENT | Transactions already renders a phone card list with sticky header + filters. Verify the same component powers Payment links, Customers, Invoices. |
| 1.8 | Detail panel — drawer on large, full-screen sheet on phone; copyable rows | PRESENT | Transaction details modal exists. Remaining: confirm drawer/sheet behaviour and reuse for links and customers. |
| 1.9 | Empty state — what/why/one button | PRESENT | `EmptyDataModel` / `NoData` exist. Verify every empty state offers the one filling action. |
| 1.10 | Form fields — inline validation, address/amount paste + validation, copy fields | PRESENT | Verify consistency during page passes. |
| 1.11 | Toasts + explicit confirm for destructive actions | PRESENT | Toast system exists. Verify destructive dialogs name the object. |
| 1.12 | Sidebar — groups, collapse to icon rail (remembered), active state, setup-progress ring, KYC nudge | DONE (2026-09-10) | Groups, rail, reveal-on-relevance and active state were PRESENT. **Built: setup-progress ring** — `Components/Layout/NewSidebar/SetupProgressItem.tsx`, a pinned "Getting started · n/4" row above the groups (ring only in the icon rail, tooltip carries the count; `sidebar-setup-progress` with `data-done` / `data-total`, `sidebar-setup-progress-count`) that resumes `/get-started` at the first unfinished step; hidden for team members and once the first payment lands. `ProgressRing` gained `hideLabel`. `useSetupProgress.hasPayment` now also reads the wallet list's `amount_in_usd` so the ring is correct on every route, not just after /dashboard has loaded stats. **KYC nudge confirmed**: header KYC pill + mobile-nav pill + dashboard `KycGraceBanner`, all one door → `/kyc` (3.10). |
| 1.13 | Header — brand switcher, ⌘K, bell with unread, + New, theme in account menu, account menu | PRESENT | All present. Verify long brand-name truncation and consistent focus rings. |
| 1.14 | Phone bottom tab bar (Home · Sell · Money · More, centred +) | DONE (2026-06, user-approved) | `Components/Layout/MobileNavigationBar` now renders **Home · Sell · [+] · Money · More** (5-col grid). Sell → /pay-links (active for /pay-links, /create-pay-link, /storefront for businesses); Money → /payouts (active for /payouts, /transactions, /invoices, /wallet); centred "+" = `CreateNewButton variant="tab"` (same Payment link / Product menu, `mobile-nav-create`); header "+ New" hidden below 768px so there is ONE create control. More rows: Your page/Dashboard · Transactions · Receipts · Payout wallets · Customers, then Developers · Referrals · Notifications · Settings · Language · Help. |
| 1.15 | Tablet icon rail with labels on hover/tap | PRESENT | Verified at 768px. |
| 1.16 | Page transitions + scroll-position restore | PRESENT | `RouteTransitionLoader` exists. Verify scroll restore when going back. |
| 1.17 | Unified auth screens (login, register, code, reset, 2FA, invite, secure-account) | DONE (2026-09-10) | One frame for every auth surface: `Components/UI/AuthLayout/AuthShell.tsx` (centered card, logo + `AuthHeaderControls` = compact language menu `AuthLangMenu` [globe + code, 6 languages, keyboard operable] + theme toggle, TrustStrip; `<title>` + noindex) and `AuthStatus.tsx` (outcome card: icon tile, title, purpose line, primary CustomButton). `/reset-password`, `/auth/secure-account`, `/auth/accept-invite` moved onto it (were: two-card legacy layout with flag dropdown; bare card without logo; raw MUI Paper/TextField/Button). Login/register now share the same header controls (user chose to ADD the language menu on all auth screens, desktop + phone). Copy: one verb — "Log in" everywhere in EN (button, description, session-timeout, landing header, page meta), PT title "Entrar"; "Go to login" buttons (no arrow) on invite + secure-account; ThemeToggle label translated. Reset: purpose line with the email, Continue disabled until both fields filled and matching. 2FA dialog backup-code submit uses CustomButton. Verified by testing_agent iteration_143 (30/30 screen×viewport×theme combos, 93%; remaining notes were spec-vs-brief nits). Note: auth surfaces persist theme under `theme-mode-public` (inherit design in `routeContext.ts`), not a separate auth key. |
| 1.18 | Guided first run — 4-step wizard replacing the auto-popping modal; "Do this later"; resumes from real data | DONE (2026-09-09, iter_140 100%) | `/get-started` (in-shell route, `Components/Page/GetStarted/*`): About you → Where payouts go (reuses OTP `WalletManagerModal`) → First payment link (form + live phone-frame `CheckoutPreview`, side-by-side ≥1400px, collapsible below) → Share it (copy / QR / share / open / download). URL `?step=`, resumes from real data (`useSetupProgress`), "Do this later" on every step, `FirstRunRedirect` sends single-brand new merchants there once per session (replaces the auto-popping `CreateCompanyModal` + `OnboardingChecklist` on /dashboard). |
| 1.19 | New-merchant dashboard — Getting-started hero, faded real dashboard beneath, progress ring | DONE (2026-09-09, iter_140 100%) | `GettingStartedHero` (SVG `ProgressRing` N/4, 4 clickable steps, ONE CTA resuming the wizard) + `DashboardPreview` (real BalanceStrip/VolumeChart/FeeTier/KPI strip faded, pointer-events none, inert, bottom mask, "Preview" label) until the first payment lands. Retired from /dashboard: `WalletSetupNudge`, `OnboardingChecklist`, `ActivationChecklist`, `EmptyHero` (files kept). |

## Checkpoint 2 — Core money pages

| # | Item | Status | Remaining / notes |
|---|---|---|---|
| 2.1 | Dashboard (established) — hero metric, KPI strip, actions row, recent transactions, right rail stacking | PRESENT | Verified desktop + mobile. Recent transactions now use the unified `StatusChip`. Remaining: shared data list. |
| 2.2 | Payment links — list as data list; detail panel with share tools (copy, QR, embed), stats, recent payments | DONE — TESTED (iteration_141, 2026-09-10) | Tip done. Slide-in detail drawer built + committed: `Components/Page/Payment-link/PaymentLinkDetailPanel.tsx` (full-screen sheet on phone), `EmbedSnippet.tsx` (embed HTML), `useLinkPayments.ts` (filters `/api/wallet/getAllTransactions` per link — no backend change), `linkStatus.ts` (status helper); wired into `PaymentLinksTable.tsx` (row click opens drawer, replaces old success/view dialog). NOT yet verified — run frontend testing_agent (open on row click, mobile full-width sheet, copy URL, QR download, embed snippet). |
| 2.3 | Create payment link — two panes (form + live checkout preview); phone collapsible preview; single "More options" disclosure; success with share tools | DONE — TESTED (iteration_141, 2026-09-10) | Audit: `/create-pay-link` already had a sticky right pane + one `<details>` disclosure + success modal, but the preview was a flat "Review Your Order" card, phones got a floating "Preview" FAB + bottom sheet (crowded the chat bubble), and the success modal had copy + QR only. Built: **`LivePreviewPanel` rebuilt as the phone-framed hosted-checkout mock** (same bezel as the wizard's `CheckoutPreview`; brand logo/initial + "Pay {brand}" + "Secured by Dynopay", description, mono amount, live `CoinChips` of the *selected* coins, CTA, expiry + fee-payer footnote; donation body keeps title/goal/presets/Donate inside the frame; `compact` prop hides the eyebrow). **Phone/tablet (<lg): inline collapsible "Show customer preview" row** under the link-type selector with a live amount summary (`mobile-preview-section` / `-toggle` / `-summary`) — FAB + bottom drawer removed. **"Advanced options" → "More options"** (`moreOptions` ×6 langs, tune icon; ⚠️/☕ emoji swapped for icons; testid `pay-link-advanced-options` unchanged). **Success modal**: amount + description summary, QR + URL + inline copy, "Copy link"/"Copied ✓" + Share / Open / Download-QR icon buttons, `EmbedSnippet` reuse, Direct-Pay block kept, currency-aware details, footer "Create another" (remounts the form via `key` in `pages/create-pay-link.tsx`) + "View all links" (testids `paylink-success-*`). `ProductQuickSell` empty row wraps on phones (button full-width). Smoke-checked 1920 + 390 light (no overflow, preview live) — success state NOT exercised (needs a real create → disposable record on the owner account). |
| 2.4 | Products — grid/list toggle, quick edit, detail panel | DONE — TESTED (iteration_141, 2026-09-10) | Storefront › Products: **grid ⇄ list toggle** (`products-view-toggle` / `products-view-{grid,list}`, remembered in localStorage `dynopay.products.view`); `ProductGridCard` (cover 4:3, status pill, price, sold; hover edit / quick-sell); list rows are now buttons. **`ProductDetailPanel`** (right drawer, bottom sheet <600px; `product-detail-panel`): image, price, stock, sold, category, **Share** (public URL + inline copy, open, QR download, quick sell — only when live + handle; hints otherwise), **Quick edit** (price + stock → PATCH `products/:id`; status segmented Draft/Live/Archived → PATCH draft / POST publish / POST archive, publish problems surfaced), footer Edit product / Archive. Copy in `common.products.*` ×6. Smoke 1920 (The Dev Store): grid → panel → share block → list persisted after reload. Not exercised: quick-edit saves (prod data), phone sheet, dark. |
| 2.5 | Transactions — data list, status chips, sticky header, sort, filters (toolbar / phone bottom sheet), scoped export; detail panel with copyable hashes, confirmations, fee breakdown, receipt link | DONE — TESTED (iteration_141, 2026-09-10) | Tip, unified status chip and copy ticks on hashes/address/callback done earlier. **Phone filter decision (user-approved): bottom sheet.** Below 768px (`useTableCardView`) the top bar shows only search + a "Filters" button with an active-count badge (`transactions-filters-btn` / `-count`); `TransactionsFilterSheet` (bottom `Drawer`, `tx-filter-sheet`) holds Source · Status (live counts) · Coin · Date range (CustomDatePicker popover opens above the sheet), footer "Clear all" + "Show N results" (`tx-filter-clear-all` / `tx-filter-apply`); apply mirrors source/status/wallet into the URL. The toolbar strip on phones shows "N results" + removable active-filter pills (`transactions-active-filter-{source,status,wallet,date}`) instead of the status chips. Desktop/tablet ≥768 unchanged. Filter predicate extracted to `txFilters.ts` (shared by page + sheet). Smoke-checked 390 light: open → select → apply → badge/pills/URL, pill removal, date popover. |
| 2.6 | Balances — clear settled vs pending, per-coin, links into transactions filtered by coin | DONE (2026-09-09) | Pending and Recent-settlement rows on /payouts are now links (role=link, Enter key, hover/focus ring) → `/transactions?wallet=<COIN>` (coin filter pre-applied via `cryptoToWalletKey`); `data-coin` on each row (`payouts-pending-row-i`, `payouts-settlement-row-i`). Verify in the test pass. |

## Checkpoint 3 — Remaining pages

| # | Item | Status | Remaining / notes |
|---|---|---|---|
| 3.1 | Your page (storefront) — clear on/off, handle claim, preview, appearance + products, guided empty state | PRESENT — audited 2026-09-09 | Screenshot audit: status/on-off, handle claim, live preview, appearance + products tabs, share tab and compare panel all exist. Left to verify in the test pass: guided empty state for a brand with no storefront yet, 390/768. |
| 3.2 | Receipts & Tax — clear tabs, period picker, preview drawer | DONE (2026-09-10) | Tip done earlier. Spot-checked tabs, period picker and the preview drawer against the quality bar. Added the missing `data-testid`s on the invoice preview drawer (`Components/Page/Invoices/InvoicePreviewDrawer.tsx`) + `pages/invoices.tsx`, and localised the remaining hardcoded drawer strings into all 6 languages (rolled into the 4.4 sweep). Verified: `tsc` 0. Not exercised by testing_agent (final smoke pass deferred at session end). |
| 3.3 | Payout wallets — card per coin, masked address with reveal/copy, network label, status; shared add/edit form; OTP-marked sensitive changes | DONE (2026-09-10) | Tip, network chips, **masked address (first 8 + last 6, `helpers/maskAddress`) with per-card eye reveal** (`wallet-address-reveal-<id>`, `data-revealed`) + `CopyInline` boxed copy of the full address (`wallet-address-copy-<id>`) done on desktop AND phone. **1920px horizontal overflow FIXED** (hero aurora glow blob in `WalletTotalHero` removed). **Verified 2026-09-10 (screenshots, owner account): ONE shared form** (`Components/UI/AddWalletModal`) serves add + edit + the wizard's payout step — same fields (optional name, coin, address, tag), same helper copy, same irreversible-address warning; edit locks the coin (lock icon) and pre-fills. **OTP marking**: edit mode already said "Changing the address requires a one-time code sent to your email." + button "Verify & save" (name-only edits skip OTP); **add mode now carries the same marker** — lock line "We'll email you a 6-digit code to confirm this wallet before it's saved." (`wallet-otp-notice`, `walletScreen.addOtpNotice` ×6). |
| 3.4 | Wallet security — plain-language explanation, protection level, change history, sign-out-everywhere, alerts | DONE — TESTED (iteration_141, 2026-09-10) | New in-shell page `/wallet/security` (`pages/wallet/security.tsx` → `Components/Page/WalletSecurity/*`; the public email-landing `/wallet-security` is untouched). Composes existing reads: 2FA status, `wallet/sudo/status`, `user/sessions`, `team/activity` (filtered to `wallet.*`) + ONE new read-only endpoint `GET /api/wallet/security/status` (Redis freeze state). Sections: frozen banner (`wallet-security-frozen-banner`), **Protection level** (3-segment meter Locked/Strong/Standard + 3 checks; “Turn on two-factor” CTA → Settings › Profile), **How your payouts are protected** (3 steps, alert email shown), **Wallet change history** (owner-only; 403 → friendly note; empty → dashed state), **Signed-in devices** (“Sign out everywhere else” = DELETE user/sessions, “Manage devices”). Entry points: “Security” button on Payout wallets header (`wallet-security-btn`, now always rendered), `WalletSecurityLink` card in Settings › Profile & Security. First-visit tip `pageTips.walletSecurity`, page title `walletSecurity_*`. Smoke 1920 + 390 (light): level Standard, 6 history rows, 10 devices, no overflow. Not exercised: sign-out-everywhere (mutation), frozen/owner-only states, dark. |
| 3.5 | Customers — searchable list, detail panel with lifetime value + payments | PRESENT (audited 2026-09-09) | Code audit: `Components/Page/Customers/index.tsx` already has the right-hand `DetailPanel` drawer (lifetime spent KPI, payments, orders, payment links, latest activity) + card list on phones; the earlier “remaining: detail panel” note was stale. Verify at 390 in the test pass. |
| 3.6 | Refer & earn — code + link as copy fields, earnings so far, 3-step how-it-works | DONE (2026-09-10) | Verified (screenshots, owner account): referral code as a copy field with tick (`referral-code-value` + `copy-referral-code-btn`, `copy-referral-link-btn`, share buttons), **3-step "How it works"** (`referral-how-it-works`: Share your code → They sign up → You both earn, with the 25 % / 50 %-off outcome cards), **earnings so far** = "Total earnings" stat (`referral-stat-total-earnings`) + `referral-earnings-card` (Credited / Pending / Withdrawn) + revenue-share card with cash-out. No build needed. |
| 3.7 | Settings — left sub-nav (tabs on phone): Profile & security, Brand, Team, Notifications, Webhooks; independent saved/unsaved state per section | DONE — TESTED (iteration_141, 2026-09-10) | Sub-nav (grouped rail / phone chip row, URL-synced) already existed. Built the missing half: `Components/Page/Settings/settingsDirty.tsx` (`SettingsDirtyProvider` + `useReportDirty(section, dirty)` + render-prop `DirtyReporter`; several forms may report into one section). Reporters: Tax (`isDirty`), Notifications (hook now exposes `isDirty` vs last-saved snapshot), Profile (name edit / email·phone change in progress / password typed), Company + Payments (`CompanySettingsDialog` `dirtySection` prop: form values ≠ initial, staged logo, account-type flip). Page: visited sections stay **mounted but hidden** so each keeps its own unsaved edits when switching (`settings-section-{key}`); amber dot on the rail item (`settings-rail-{key}-unsaved`) + "Unsaved changes" chip in the header (`settings-unsaved-chip`); `beforeunload` prompt + in-app route guard dialog "Leave without saving?" (`settings-leave-dialog` / `-cancel` / `-discard`) — section switches inside /settings are exempt. Smoke-checked 1920: toggle → dot+chip, switch away/back keeps the edit, nav to Developers → dialog, Keep editing stays, Discard navigates. |
| 3.8 | Developers — masked API keys with copy, webhook console, quick-start snippets; keep redirect banner | DONE (2026-09-10) | Tip + `CopyInline` on keys done earlier. Verified (screenshots, owner account): **Live + Test secret keys are masked by default** (first 10 + last 4, `••••` in between) with copy + eye reveal; publishable keys section; **webhook console** (`webhook-console`: endpoint URL + save, masked signing secret + regenerate, send test event, delivery toggle, per-event subscriptions, auto-disabled banner with reason + re-enable) on the Webhooks tab; Events log + Docs tabs with quick-start snippets; legacy `/settings?section=api-keys|webhooks` redirects kept. No build needed. |
| 3.9 | Notifications — grouped by day, unread emphasis, tap opens matching detail, mark-all-read | DONE — TESTED (iteration_141, 2026-09-10) | New `NotificationInbox.tsx` replaces the flat list: groups **Today / Yesterday / This week / Earlier** (`notifications-group-{bucket}` + per-group unread count), rows are real buttons (Enter/Space), family icon + tone (payment / kyc / wallet / team / api key / company / summary), stronger unread emphasis (tinted card + dot + bold), tap-through hint. `targetFor()` in NotificationPage: payment → transaction drawer (as before); kyc → `/kyc`; wallet → `/wallet/security`; security → Settings › Profile; team → Settings › Team; api_key → `/developer-keys`; company → Settings › Company; weekly summary → `/transactions`. Tabs + mark-all-read translated (`notifications.inboxTab/settingsTab/markAllRead/…` ×6). Smoke 1920: 3 groups / 20 items render, unread + hints visible. Not exercised: non-payment tap targets (none on the owner account), phone. |
| 3.10 | KYC — status page: what's needed, why, how long; single continue | DONE — TESTED (iteration_141, 2026-09-10) | New in-shell page `/kyc` (`pages/kyc/index.tsx` → `Components/Page/Kyc/*`; `/kyc/complete` stays the bare Veriff return). `useKycPage` derives ONE view from `useKycGate` + `/kyc/requirements` + `/kyc/history`: verified · in_review · retry · action_needed · not_needed. Hero (`kyc-status-hero[data-view]`): state title/body, chips (takes ~time, volume/threshold %, deadline date, submitted date), and a single Continue (`kyc-continue-btn`: Start/Continue verification → `startVerification`; Try again → POST `/kyc/resubmit`; Verify now when under threshold; none when verified/in review). “What you'll need” (3 docs, translated) + “How it works” (3 steps + Veriff note) hidden once verified; “Verification history” only when records exist. **One door**: header KYC pill, mobile-nav KYC pill and the dashboard `KycGraceBanner` now route to `/kyc` instead of POSTing to Veriff directly. Tip `pageTips.kyc`, title `kyc_*`, `/kyc` added to private (noindex) prefixes. Smoke 1920 + 390: owner is verified (hero + 3 approved records). Not exercised: action_needed / retry / not_needed states (need a non-verified account), Veriff launch. |
| 3.11 | Help & Support — in the shell, searchable, consistent chat entry | PRESENT — audited 2026-09-09 | Screenshot audit: renders inside the shell with search, category tiles and the same chat bubble entry as every page. Verify article deep-links + 390 in the test pass. |
| 3.12 | Auth pages unified | DONE (2026-09-10) | See 1.17. |

## Checkpoint 4 — Cross-device polish sweep

| # | Item | Status | Remaining / notes |
|---|---|---|---|
| 4.1 | Every in-scope page at 390 / 768 / 1280 / 1920 × light / dark; no horizontal overflow, no clipped text | DONE (2026-09-10) | Scripted sweep `scripts/qa/responsive_sweep.mjs` (logs in once, 20 merchant routes × 4 widths × 2 themes = 160 combos, checks `scrollWidth > innerWidth`, clipped/nowrap text, raw i18n keys, JS errors; `--shots` for contact sheets via `scripts/qa/contact_sheet.py`). Result: 0 overflow, 0 clipped text, 0 raw keys, 0 JS errors. Fixed from the visual pass: `/get-started` + `/kyc` were forced LIGHT in dark mode (missing from `INAPP_PREFIXES` in `utils/theme/routeContext.ts` + `_document.tsx`); Settings phone chip row now auto-scrolls the active chip into view on deep links; storefront handle prefix no longer eats the input at 390 (`CreatorPageSettings`). Results in `memory/reports/sweep/results.json`. |
| 4.2 | Motion — 150–250ms ease-out, honours reduced-motion | DONE (2026-09-10) | App-wide sweep complete: every transition/animation clamped to the 150–250 ms band (fixed all >250 ms outliers) and `prefers-reduced-motion` is strictly honoured across the app (durations collapse to ~0, non-essential animation disabled). Audit JSONs in `memory/reports/motion/`. Verified: `tsc` 0. Final frontend testing_agent smoke pass deferred at session end. |
| 4.3 | Empty / loading / error states everywhere | DONE (2026-09-10) | Swept every merchant page on the empty brands (QA Throwaway Brand 2 #179, Nameword #165) at 390 + 1920, light + dark (`responsive_sweep.mjs --company=179`) plus scroll-through shots (`scripts/qa/scroll_shots.mjs`). Every list/panel has a friendly empty state; no blank cards. Fixed: /wallet stacked FOUR redundant "no wallets" messages on a phone (page-level warning banner with 10px text removed — the empty state + reuse nudge carry it; remaining no-brand banner bumped to 13px); Settings › Notifications now opens on the preferences tab, not the Inbox (`NotificationPage initialTab`), switches carry `notification-pref-*` testids. Noted, not changed: KYC page is per-brand (an empty brand shows "Not needed yet" while account-wide history lists 3 approvals) — by design. |
| 4.4 | Copy review in all 6 languages | DONE (2026-09-10) | App-wide sweep complete: automated manifest (`scripts/qa/i18n_build_manifest.py`) + missing-key detector (`scripts/qa/i18n_missing_keys.py`) located hundreds of hardcoded strings across ~18 pages; English JSONs patched and translations applied to DE / FR / ES / PT / NL for 100% coverage. Language switching logic in `i18n.js` + `pages/_app.tsx` patched. Sweep logs in `memory/reports/i18n/`; manifest at `scripts/i18n_manifest.json`. Verified: `tsc` 0. Final frontend testing_agent smoke pass deferred at session end. |
| 4.5 | Keyboard-only navigation + visible focus on every page | DONE (2026-09-10) | Audit script `scripts/qa/keyboard_audit.mjs` (logs in; per page: cursor-pointer elements that are not focusable, positive tabindex, unnamed icon buttons, unlabeled inputs, Tab-walk for missing rings / focus on hidden or off-screen elements; run at 1280 + 390; results `memory/reports/keyboard/audit-*.json`). Fixed: **skip-to-content** link + `header`/`nav[aria-label]`/`main#main-content` landmarks (`Containers/Client`); sidebar items were plain divs → `role=link`, tabIndex, `aria-current`, Enter/Space; company selector + user menu triggers keyboard-openable, user menu is a real `role=menu` (first item focused, arrows cycle, Escape returns focus, `menuitem`s); phone bottom tabs focusable (`mobile-nav-{home,payments,wallet,more}`); hidden phone language list no longer traps focus (`visibility` when closed); list rows/cards keyboard-operable via `helpers/a11y.ts rowKeyProps` (payment links, transactions row + card, customers row + card, invoices row + card; payouts rows already were); custom dropdown triggers open with Enter/Space/ArrowDown (currency, crypto coin, expiry, rows-per-page, coin Select/Clear/Show-all are real buttons); dashboard/help text links and referral pill actions became `<button>`s (payout method = radiogroup); icon-only buttons named (publishable-key toggle/edit/revoke, pagination prev/next, phone export, search, referral copy/share; CustomButton auto `aria-label` when `hideLabel`); decorative icon "buttons" removed from the tab order (Profile panels, help footer, pay-links search); InputField always associates its label (`fieldId`); checkbox/radio/switch paint the focus ring on the visible control (`globals.css`). Verified by testing_agent iteration_144 (skip link, landmarks, sidebar, menus, rows, dialogs, phone tabs, no hidden focus stops) + main-agent re-check of its 4 findings (switch ring, share button name, `mobile-nav-home`, menu semantics). |
| 4.6 | Fresh account: sign-up → shared payment link on a phone without instructions | DONE (2026-09-10) | Two REAL sign-ups at 390×844 through the UI (OTP read from Redis via `backend/scripts/read_otp.cjs`; wallet-sudo code recovered via `backend/scripts/read_sudo_otp.cjs`), driven with `scripts/qa/pw_driver.mjs`; both hard-deleted afterwards with `backend/scripts/scratch_fresh_signup_cleanup.js` (deletes every row by user_id/company_id + the Tatum pool-address subscriptions it provisioned). Snags found → fixed: (1) "Sell products" sign-ups were routed to the heavy `/pay-links/products/new` form with a fee-free modal + 2 header chips on top — now `/get-started` (`helpers/verticalOnboarding.ts`); (2) fee-free "You're in!" modal stayed OPEN after its CTA navigated (Client layout persists) — now closes; it is suppressed on the wizard and, once a link exists, its CTA reads "Share your payment link" → /pay-links; (3) bottom tab "Your page" had an EMPTY icon for individual accounts (`renderIcon` lacked creator/products cases); (4) Add-wallet modal silently required a Wallet Name (Continue stayed disabled, no hint) — name is optional, falls back to "<COIN> wallet", helper says Optional; coin selector placeholder "Bitcoin (BTC)" looked pre-selected → "Choose a coin…"; (5) wizard step 2 on a phone hid the primary button below three explainer cards — wallets block + footer now come first on xs, cards beneath; (6) wizard Share step actions wrapped to two lines at 390; (7) resumed Share step lost the amount (`linkFromRecord` ignored `base_amount`); (8) `ClaimHandleBanner` competed with the Getting-started hero and its URL overflowed at 390 — now waits for the first payment, wraps anywhere. Verified e2e by testing_agent (iteration_142, 97%; the one "defect" — CTA not on /pay-links after 4s — was the 11.8s dev compile of /pay-links, see frontend log). |

---

## Flagged items & decisions needed

1. ~~Payout wallets page — horizontal-overflow element at 1920px (`mui-33muq1`)~~ — FIXED (hero glow blob removed, see 3.3).
2. ~~**Mobile Transactions source tabs** — scroll horizontally with an edge-fade. Decide: keep, or move filters to a bottom sheet per plan §4.~~ — RESOLVED by 2.5 (user-approved bottom sheet below 768px; source · status · coin · date live in the sheet).
3. ~~**Phone bottom-bar labels** — plan says Home · Sell · Money · More with a centred "+"; app has Home · Payments · Wallet · More with "+" in the header.~~ — RESOLVED 2026-06: user chose the plan's set; shipped as 1.14 (Money lands on Balances /payouts).
4. Social sign-in shows `redirect_uri_mismatch` on the preview domain — environment, not a UI task.

## Beyond the plan (shipped alongside, 2026-09-10)

- **Gateway health strip** on /dashboard (user-chosen home for the "health dashboard" backlog item): `Components/Page/Dashboard/v2026/GatewayHealthStrip.tsx` + `gatewayHealth.ts`, fed by NEW public `GET /api/status/gateway` (`backend/controller/status/gatewayController.ts`; 20 s in-process cache) = Payments (Tatum circuit + monitor) · Rates (Binance WS if live, else the always-on Tatum/CoinGecko cache with "updated Xm ago") · Webhooks · API (latency) + overall. Shown under the actions row in BOTH the established and the getting-started layouts; 2×2 on phones; "Status page ↗" → `/system-status`. Testids `dash2026-gateway-health[data-overall]`, `gateway-overall`, `gateway-check-{payments,rates,webhooks,api}[data-status]`, `gateway-status-page-link`. Copy `dashboardLayout.gateway.*` ×6.

## Recommended execution order (next)

1. ~~Status chip unification (1.4)~~ — DONE.
2. ~~Copy everywhere (1.3)~~ — DONE.
3. ~~Guided first-run wizard + new-merchant dashboard (1.18, 1.19)~~ — DONE (2026-09-09).
4. ~~Payment links detail panel (2.2) + Create-link two-pane preview (2.3)~~ — CODE DONE, both awaiting the frontend testing_agent pass (2.2: row click → drawer, phone sheet, copy/QR/embed; 2.3: live preview at 1920/768/390 both themes, collapsible toggle, More options, success modal share tools + Create another).
5. ~~Transactions phone filter decision (2.5) + Settings sub-nav (3.7)~~ — CODE DONE (bottom-sheet filters <768px; per-section unsaved state + leave guard), awaiting the same frontend testing_agent pass as 2.2/2.3.
6. ~~Wallet security 3.4 + KYC 3.10 with first-visit tips~~ — CODE DONE, testing pending with the rest.
7. ~~3.9 Notifications inbox + 2.4 Products grid/panel/quick-edit~~ — CODE DONE; 3.1 Storefront + 3.11 Help audited PRESENT.
8. ~~1.1 tokens source, 2.6 coin-filtered links, 3.5 audit~~ — DONE 2026-09-09 (user chose to defer the 4.1 sweep).
9. ~~**Full frontend testing_agent pass** over everything marked CODE DONE (2.2, 2.3, 2.4, 2.5, 2.6, 3.4, 3.7, 3.9, 3.10) + B-block verifications (3.2, 3.3, 3.6, 3.8)~~ — DONE 2026-09-10: iteration_141 (95%, 3 minor defects fixed: `paylink-detail-copy` tick attr, `mobile-preview-summary` always rendered with $0.00 placeholder, `preview-standard-total` spacing) + iteration_142 regression (97%). Then 4.1 / 4.3 / 4.6 — DONE (see Checkpoint 4).
10. ~~1.17/3.12 unified auth screens~~ — DONE 2026-09-10 (AuthShell + language menu on every auth screen).
11. ~~4.5 keyboard-only navigation + visible focus~~ — DONE 2026-09-10.
12. ~~4.2 motion sweep; 4.4 six-language copy review; B-block 3.2 Receipts preview drawer spot-check~~ — DONE 2026-09-10.
13. ~~Close-out: verify-and-close 3.3 / 3.6 / 3.8, build the 1.12 sidebar ring, mark 1.2 complete, regenerate `/ux-plan/status.html`~~ — DONE 2026-09-10 (session 5). **Every plan row is now DONE or PRESENT-verified.**
14. **Only open follow-up (deferred by the user):** ONE final frontend testing_agent smoke pass — 4.2 motion + reduced-motion, 4.4 raw-key scan ×6 languages, 3.2 Receipts drawer, the sidebar ring (empty brand 179 / 165 vs The Dev Store), the add-wallet OTP marker, the gateway health strip (1920 + 390, both themes) — then Save to GitHub to ship.

## Access, constraints & verification

- Preview: pod-specific — see `/app/memory/test_credentials.md` (re-verify after any rebuild).
- Test accounts: see `/app/memory/test_credentials.md` (owner merchant + super-admin). Brands on the owner account: **The Dev Store** (populated) and **QA Throwaway Brand 2** (empty — useful for empty-state checks).
- Scope guardrails: merchant app only; front-end only, existing APIs; evolve-not-rebrand (indigo, light + dark); no feature removed.
- Data guardrails: SAFE MODE against the production DB — prefer read-only; anything that writes uses the owner account + disposable records only.
- Fresh pod: `.env` files are gitignored — restore with `bash scripts/pod-bootstrap.sh --pass '<vault passphrase>'` (passphrase is in the credentials file, never in this doc).
- Every change verified at 390 / 768 / 1920 in both themes with a horizontal-overflow check before hand-off.

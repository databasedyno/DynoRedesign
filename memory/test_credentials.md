# Test Credentials

## Preview Environment (Emergent)
- URL: https://62fabf57-7aa4-49d7-b5cf-93ca135cec6b.preview.emergentagent.com (CURRENT — session 37, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = 62fabf57-7aa4-49d7-b5cf-93ca135cec6b = authoritative URL for THIS container. On boot: ROOT node_modules + BACKEND node_modules + all 3 .env were MISSING (.next/standalone existed but stale from prior container). SEQUENTIAL yarn installs: /app root (76s) then /app/backend (29s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env (216 lines) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / NEXT_PUBLIC_API_DOCS_URL / REACT_APP_BACKEND_URL) → 62fabf57 preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET A4Nn6omEk92X5rKySGKdr0YH5pzGIuBrt9Zu5aN/4zI=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \\n-escaped (double backslash preserved exactly as user pasted; normalizePrivateKey collapses to real \n); PORT omitted (server.py injects 3300 via ts-node --transpile-only server.ts); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in Node stdout: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped; /health background_jobs.eligible=false, is_leader=false. next build standalone OK (BUILD_EXIT=0, 438 kB shared JS). Frontend start script symlinks /app/.env + /app/.next/static into .next/standalone AND bakes NEXT_PUBLIC_* at build time → MUST build AFTER writing .env (done). Health: internal :8001 /health = 200 (database=connected, redis=connected, tatum operational CLOSED 40 rates), /api/csrf-token = 200; internal :3000 / = 200 + static chunk 200. External / /api/csrf-token /auth/login /fees = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present, screenshot confirms login card renders); bad-creds POST /api/user/login = 401 "Invalid email or password". Expected quirks: Binance geo-blocked (WS 451) → Tatum fallback (9 cached prices → 40 refreshed); sshpass missing → SSH tunnel disabled. NO CODE CHANGES this session — pure environment provisioning.)

- URL: https://crypto-checkout-39.preview.emergentagent.com (SUPERSEDED — session 36 continued: Phase C + invoice/tax accuracy fixes SHIPPED. Frontend: NEW TryFirstPaymentCard on /developer-keys — masked cURL card with reveal-to-copy, only mounts when merchant has an active dpk_test_ sandbox key (env=development, sandbox_mode=true); 15 new i18n keys × 6 locales. data-testids: try-first-payment-card, try-first-payment-reveal, try-first-payment-copy, try-first-payment-snippet, try-first-payment-response. Backend: 5 invoice/tax bugs fixed in backend/controller/invoiceController.ts + backend/controller/taxController.ts — (A) invoice total_usd now includes % tx fee, (A') VAT base defaults to service_fee (fixed+txFee), legacy gross behavior available via INVOICE_VAT_ON_GROSS=true env, (B) EU VAT fallback now uses per-country FALLBACK_TAX_RATES (DE=19, HU=27, LU=17, etc.) instead of hard-coded 23%, (C) taxController TAX_ACRONYMS aliased to TAX_TYPE_ACRONYMS (VAT/IVA/GST/Tax) not TAX_ID_ACRONYMS (EIN/CUIT/CNPJ), (D) invoice FX now baseCurrency→preferredCurrency not USD→preferred, (E) sanitized invoice returns `provider_vat_id` matching model field (was provider_tax_id → undefined). Testing agent (deep_testing_backend_v2) also fixed 2 runtime bugs during verification: (1) processing_fee parseFloat wrapper on fixed_fee to avoid .toFixed on string decimal, (2) taxController external-API fallback logic when API returns 0/null. Backend test result: 12/12 PASS ✅. Next build OK 75s, /health 200, external / and /developer-keys 200. NO frontend testing agent run — awaiting user approval.)

- (SUPERSEDED preview URL — session 36 initial provisioning: URL: https://crypto-checkout-39.preview.emergentagent.com (CURRENT — re-provisioned 2026-07-12, session 36, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = 52b445cf-1923-4fba-8f62-af260505dbd6 = authoritative URL for THIS container. node_modules + all 3 .env were MISSING on boot (.next/standalone existed but stale) → SEQUENTIAL yarn installs: /app root (78s) then /app/backend (30s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env (172 lines) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → 52b445cf preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET QUmx6mqI4JmuSvoz1kwO2Oh5AJzqVdv5MvrgMt4kIKU=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \\n-escaped (double backslash preserved exactly as user pasted; dotenv double-quoted + normalizePrivateKey collapses to real \n); PORT omitted (server.py injects 3300 via ts-node --transpile-only server.ts); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified: /health background_jobs.eligible=false, is_leader=false. next build standalone OK (Done in 118s, 436 kB shared JS). Health: internal :8001 /health /api/csrf-token = 200; internal :3000 / = 200; /health database=connected redis=connected tatum operational(CLOSED, 0 failures, 9 cached prices). External / = 200, /api/csrf-token = 200, /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present); bad-creds POST /api/user/login = 401. Expected quirks: Binance geo-blocked (WS 451) → Tatum fallback; sshpass missing → SSH tunnel disabled. NO CODE CHANGES this session — pure environment provisioning.)

- (SUPERSEDED preview URL — session 35, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = bd251826-361d-4dcd-8134-9495baf74e48 = authoritative URL for THIS container. NOTE: the older "secure-pay-40.preview.emergentagent.com" alias returns 404 for this container — use the bd251826 UUID URL. node_modules + all 3 .env were MISSING on boot (.next existed but stale) → SEQUENTIAL yarn installs: /app root (75s) then /app/backend (28s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env (230 lines) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → bd251826 preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET FkWi9fek0ZkYiT6+Zn2SJuvR5YiJqVvlbN5n23gsuvY=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY written with single \n-escape (user pasted \\n; dotenv double-quoted + normalizePrivateKey both collapse it); PORT omitted (server.py injects 3300 via ts-node --transpile-only server.ts); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in Node stdout: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped; /health background_jobs.eligible=false. next build standalone OK (Done in 103s, 436 kB shared JS; .next/standalone/.next/static + .env symlinks resolve after frontend restart). Health: internal :8001 /health /api/csrf-token = 200; internal :3000 / = 200; /health database=connected redis=connected tatum operational(CLOSED, 0 failures, 40 rates refreshed). External / = 200, /api/csrf-token = 200, /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present); bad-creds POST /api/user/login = 401 "Invalid email or password". Expected quirks: Binance geo-blocked (WS 451) → Tatum fallback (9 cached prices); sshpass missing → SSH tunnel disabled. NO CODE CHANGES this session — pure environment provisioning.)

- (SUPERSEDED preview URL — session 34 (fork carry-over of session 33), continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = 40b4ff19-5dd6-4c10-9148-72e7af6c58cf = authoritative URL for THIS container. node_modules were MISSING on boot → SEQUENTIAL yarn installs: /app root (80s) then /app/backend (30s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → 40b4ff19 preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET VCR+D2dcpZECjCBDtdDnggABX4U/qxaYkNJfBZneRJA=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \n-escaped (single backslash — .env is not JSON, normalizePrivateKey collapses); PORT omitted (server.py injects 3300); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in /health: background_jobs.eligible=false. next build standalone OK (18 routes, 436 kB shared JS). Health: internal :8001 /health /api/csrf-token = 200; internal :3000 / = 200; /health database=connected redis=connected tatum operational(CLOSED, 9 cached prices, 0 failures). External / = 200, /api/csrf-token returned CSRF token, /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present); bad-creds POST /api/user/login = 401 "Invalid email or password". Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled. NO CODE CHANGES this session — env setup + refresh of /app/memory/AUTO_API_KEY_PROVISIONING_PLAN.md into an execution-ready checklist (Phase A backend + Phase B frontend + Phase C enhancement) awaiting user "go".)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 31, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = 8d0200cf-65f6-4dfd-a2d1-85d5a5960c0f = authoritative URL for THIS container. node_modules were MISSING on boot → SEQUENTIAL yarn installs: /app root (76s) then /app/backend (29s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env (172 lines each) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → 8d0200cf preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET duKb1gThy6PZVtn+fL7irGt5KMD8J/wYHB/oVCbMp28=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \\n-escaped (double backslash preserved exactly as user pasted — normalizePrivateKey collapses \\n & \n); PORT omitted (server.py injects 3300); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in /health: background_jobs.eligible=false. next build standalone OK (BUILD_EXIT=0, 436 kB shared JS; launched via `nohup bash -c '...next build > log 2>&1'` — output is Node block-buffered when redirected so log stays empty for ~15s, that is NORMAL, not a hang; setsid+disown got killed when the tool call ended so plain nohup& is the working launch). Health: internal :8001 /health /api/ = 200; internal :3000 / = 200; /health database=connected redis=connected tatum operational(CLOSED, 9 cached prices). External / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present, screenshot confirms login card renders); bad-creds POST /api/user/login = 401. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback. NO CODE CHANGES this session — pure environment setup only.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 30, re-provisioned 2026-07-12, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = e9437496-85c4-4bf7-b609-65d7d0c69095 = authoritative URL for THIS container. SEQUENTIAL yarn installs — /app root (76s) then /app/backend (30s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env (170 lines each) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL) → e9437496 preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET D6rR6JjZfIAoSOGHPra3EoFPLiwXWkpGfff3thtJsZ4=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \n-escaped (single backslash — .env is not JSON); PORT omitted (server.py injects 3300); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in /health: background_jobs.eligible=false. next build standalone OK. Health: internal :8001 /api/ /health /api/csrf-token = 200; internal :3000 / /auth/login = 200; /health database=connected redis=connected tatum operational(CLOSED). External / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present); bad-creds POST /api/user/login = 401 "Invalid email or password". Tatum returned 40 rates. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 29, re-provisioned 2026-07-12, container 8640b4d6-3836-49fc-9b99-9884cd83b0b7 = authoritative URL for THIS container. SEQUENTIAL yarn installs — /app root (75s) then /app/backend (29s). Wrote IDENTICAL /app/backend/.env + /app/.env + /app/frontend/.env (170 lines each) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL) → 8640b4d6 preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET xJF9ouMqEG4eaQtLA4zR6cH8iReHg9L/WiIjqIfeFUg=; GitHub creds (Ov23liBuaGCFqNpp2QzW); user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \\n-escaped (single backslash — .env is not JSON); PORT omitted (server.py injects 3300); OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in backend logs: "BACKGROUND JOBS DISABLED", error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped; /health confirms background_jobs.eligible=false. next build standalone OK. Health: internal :8001 /api/ /health /api/csrf-token = 200; internal :3000 / /auth/login = 200; /health database=connected redis=connected tatum operational(CLOSED). External / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present); bad-creds POST /api/user/login = 401 "Invalid email or password". Tatum returned 40 rates in 7.6s. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 26, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = acf1d2d3-b7ea-43b1-b597-d955b1562de8 = authoritative URL for THIS container. node_modules were MISSING on boot → SEQUENTIAL yarn installs: /app root (76s, next present) then /app/backend (29s). Wrote IDENTICAL /app/backend/.env + /app/.env (170 lines each) from user continuation env: all app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL) → acf1d2d3 preview URL; CORS_ALLOWED_ORIGINS = preview host FIRST + dynopay.com + checkout.dynopay.com; fresh NEXTAUTH_SECRET A1MjM9b4MjNIXGZapc2Olc+TW7VJAuGRhAmfsbsjJZU=; GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept \\n-escaped; PORT omitted (server.py injects 3300); OPENAI_API_KEY included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — /health confirms background_jobs.eligible=false. next build standalone OK (rc=0, 18/18 static pages; build had to be launched via `setsid /tmp/run_build.sh` detached — plain `nohup &` got the tool session OOM-killed twice). Health: internal :8001 /health /api/ /api/csrf-token + :3300 /health + :3000 / = 200; /health database=connected redis=connected tatum operational(CLOSED). external / /api/csrf-token /auth/login /fees = 200. Tatum 9 cached prices, Binance geo-blocked (WS 451) → fallback (expected); sshpass missing → SSH tunnel disabled (expected). **Session 26 task = responsive/cross-device audit ONLY (no code changes). Verified via own Node Playwright (headless_shell @ /pw-browsers/chromium_headless_shell-1208) with REAL device emulation (390/768/1440) + ancestor-overflow-clip detection: ALL 5 public pages (/ /fees /documentation /blog /auth/login) × 3 viewports → horizontal overflowPx=0, 0 culprits. Visually confirmed all mobile/tablet/desktop renders clean (mobile nav collapses; tablet shows hamburger; login card + SSO buttons well-spaced). NOTE: the frontend_testing_agent's automated 'horizontal overflow' (marquee/MuiDrawer off-screen) and 'login overlaps (26 on desktop)' were FALSE POSITIVES — naive bounding-box detection ignoring overflow:hidden clipping + z-stacked decorative card backgrounds / split-panel layout. Screenshots: /tmp/resp/*.jpg.**)
- SESSION 26 UX FIXES (2026-07-11, frontend-only, rebuilt+verified by frontend_testing_agent): (1) Checkout CTA — i18n key `checkout.cryptocurrency` (real /pay checkout btn, pages/pay/index.tsx:1509) + `previewPayCrypto` (LivePreviewPanel) changed "Cryptocurrency" → "Pay with Cryptocurrency" in all 6 locales (en/es/fr/de/nl/pt common.json + createPaymentLinkScreen.json). (2) Fee wording `checkout.processingFeesIncluded` "Processing fees included" → "Fees covered by merchant" (6 locales). (3) **REAL FONT BUG FIX**: `fontFamily="Space Grotesk"` was used in 7 files but Space Grotesk is NOT loaded anywhere (app only self-hosts Geist Sans/Mono + Manrope/Urbanist) → checkout RESULT screens (success.tsx, bankTransferCompo.tsx, UI/OverPayment, UI/UnderPayment, UI/Loading, pay/terms-of-service, pay/aml-policy) rendered SERIF in production. Replaced all 47 occurrences with `var(--font-sans)` (Geist). (4) Documentation (pages/documentation.tsx): added desktop sidebar search (filters SECTIONS), mobile/tablet sticky Autocomplete "Search or jump to a section…", back-to-top Fab (bottom:{xs:88,md:96} right:24 z:1300 — placed ABOVE the chat bubble to avoid click interception), and cURL/Node.js/Python language tabs on Getting Started (role="tab", data-testid=gs-lang-{curl|node|python}, GS_EXAMPLES const). (5) Dashboard HeroMetrics.tsx: decline delta chips now neutral grey (text.secondary) instead of alarm-red; delta hidden when tile value is 0. NOTE: audit items "network ambiguity on stablecoin chips" + "verify success page font" were checked against REAL code — real checkout already has crypto.sendOnlyWarning/memoWarning (network safe) and the serif was the Space-Grotesk bug (now fixed). Demo pages (/pay/demo etc.) auto-fixed via shared i18n keys.

- URL: https://crypto-checkout-39.preview.emergentagent.com (CURRENT — re-provisioned 2026-07-11, session 25, continuation setup on a FRESH container. supervisor backend APP_URL = 6857c591-3ef0-4606-a4bd-74b040571cd5.preview.emergentagent.com = authoritative URL for THIS container. Steps: SEQUENTIAL yarn installs — /app root (76s, 551 pkgs) then /app/backend (30s, 572 pkgs); wrote /app/backend/.env + /app/.env from user continuation env, then session-25 updated all app URLs (FRONTEND_URL / NEXT_PUBLIC_BASE_URL / SERVER_URL / NEXTAUTH_URL in /app/.env; SERVER_URL / FRONTEND_URL / CORS_ALLOWED_ORIGINS in /app/backend/.env) → 6857c591 preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK (65 routes, 429KB shared JS). Health: internal :8001 /api/ /health /api/csrf-token = 200; internal :3000 / /developer-keys /documentation /v1/embed.js /elements-test.html = 200. Tatum returned 40 rates. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled. **Session 25 delivered Phase 3(b) frontend completion: (3C) new ElementsWidgetCard section on /developer-keys with copy-paste HTML+React snippet + live preview toggle that lazy-loads /v1/embed.js from window.location.origin and mounts a Dynopay(pk).elements().create('crypto',{amount:5}); (3D) new "Elements — Inline Crypto Widget" section in DEVELOPER_INTEGRATION_GUIDE.md (858 → 1,058 lines) + 3 new endpoint cards + SECTIONS entry + new cyber-lime "publishable-key" AuthBadge variant in /documentation.tsx; (Polish) public/v1/embed.js grew 32.6k → 40.1k adding: (a) auto dark/light via prefers-color-scheme when appearance.theme unset/'auto', (b) i18n LOCALES table (en/es/fr/pt/hi) + appearance.locale + appearance.labels overrides — all rendered strings interpolated via new _t() helper, (c) named APPEARANCE_PRESETS (default/stripe-like/flat/minimal). Backward-compat verified: window.Dynopay still callable AND still has initEmbeddedCheckout/openCheckout/redirectToCheckout. Screenshots: /app/.screenshots/{docs_elements,sdk_i18n_presets,elements_dashboard,elements_preview_live}.png.**)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 24, re-provisioned 2026-07-11, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = 4e39dada-7833-4544-b988-09c2688f90fb = authoritative URL for THIS container. Steps: SEQUENTIAL yarn installs — /app root (79s, 551 pkgs) then /app/backend (29s, 572 pkgs); wrote 3 .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → 4e39dada preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET hR8mQwLxT2vY7pKjZ9nBcVfEuAsGdHiXoM4NkP1TrWU=, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK (18/18 static pages, 429KB shared JS). Health: internal :8001 /api/ /health /api/csrf-token + :3000 / /auth/login = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present), bad-creds POST /api/user/login = 401 "Invalid email or password". Tatum returned 40 rates. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled. **Session 24 delivered Elements Inline Widget (Phase 3b) backend + SDK — 17/17 backend tests pass; T4 reserved ONE real merchant-pool address (USDT-TRC20 @ TRyk74od7FfrRYeopp1azu26HcxKdb6zj2, $5, RESERVED, no fund movement).**)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 23, re-provisioned 2026-07-11, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = d854bbb4-1c8d-4448-ad1b-28975a02c5ea = authoritative URL for THIS container. Steps: SEQUENTIAL yarn installs — /app root (77s) then /app/backend (29s); wrote 3 .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → d854bbb4 preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET hVQrTz3PmL5b9Xk8sYy2NcRqW7ejFa1BvGdKuHoIxAM=, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK. Health: internal :8001 /api/ /api/csrf-token + :3000 / /auth/login = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present), bad-creds POST /api/user/login = 401 "Invalid email or password". Tatum returned 40 rates. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 22, re-provisioned 2026-07-11. Steps: PARALLEL yarn installs — /app root (already up-to-date, 0.4s — node_modules preserved from image) + /app/backend (28s); wrote 3 identical .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → 4f161ef9 preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET NaWfnKKf93dyfmPvaLv/PdrYgNdEBOToRAuYWvRovTA=, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK. Health: /health database=connected redis=connected background_jobs.eligible=false; internal :3300 /health + :8001 /health /api/ /api/csrf-token + :3000 / /auth/login = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present), bad-creds POST /api/user/login = 401 "Invalid email or password". Tatum returned 40 rates. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 21, re-provisioned 2026-07-11, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = f6813974... = authoritative URL for THIS container. Steps: SEQUENTIAL yarn installs — /app root (77s) then /app/backend (29s); wrote 3 .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → f6813974 preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET Fm1GEYcY5vsVgTnnVRJsMO11EGKl4N2jHlh8AyTZqiE=, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true). next build standalone OK. Health: /health database=connected redis=connected tatum operational (CLOSED) background_jobs.eligible=false; internal :3300 /health + :8001 /health /api/ /api/csrf-token + :3000 / = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present), bad-creds POST /api/user/login = 401. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — re-provisioned 2026-07-10, session 20, continuation setup on a FRESH container. preview_endpoint env + supervisor backend APP_URL both = f490872d... = authoritative URL for THIS container. Steps: SEQUENTIAL yarn installs — /app root (95s) then /app/backend (30s); wrote 3 .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → f490872d preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET 3j+/fiNC5TtXvk9Jw8zt50jUDXmUG8ooncS69lRvEZA=, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped (normalizePrivateKey collapses \\n & \n), PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK (✓ Compiled, 18/18 static pages), public/ 12 top-level entries intact after build (shim COPIES public — never symlink), .next/static + .env symlinks OK. Health: /health database=connected redis=connected tatum operational (CLOSED, 40 rates) background_jobs.eligible=false; internal :3300 /health + :8001 /health /api/ /api/csrf-token + :3000 / /auth/login = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google/GitHub" present), bad-creds POST /api/user/login = 401. One-time non-fatal quirk: uvicorn --reload WatchFiles threw OSError "OS file watch limit reached" once during node_modules install spike (inotify max_user_watches=12288, sysctl not permitted to raise) — backend pid recovered & is STABLE, not crash-looping. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled.)
- URL: https://crypto-checkout-39.preview.emergentagent.com (CURRENT — re-provisioned 2026-07-10, continuation setup on a FRESH container. SEQUENTIAL yarn installs — /app root (78s) then /app/backend (29s); wrote 3 .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → 9c06619d preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET OyMAm5ZwoTEJHKGOfukan3QPuRavav6CFwKtqWB3zbk=, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK (rc=0, 104s), public/ 12 top-level entries intact after build (shim COPIES public — never symlink), preview URL baked into pages/auth/login-*.js + register-*.js chunks. Health: internal :3300 /health + :8001 /api/ /health /api/csrf-token + :3000 / /auth/login = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn + "Continue with Google" + "Continue with GitHub" present), bad-creds POST /api/user/login = 401. Tatum returned 40 rates → Railway PG + Redis + Tatum operational. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko fallback; sshpass missing → SSH tunnel disabled.)

- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — earlier sessions, re-provisioned 2026-07-10, continuation setup on a FRESH container. preview_endpoint env var + supervisor backend APP_URL both = a6e502cd... (this is the authoritative URL for THIS container; the "payment-hub-587" alias referenced in older notes was per-session UUIDs). Steps: SEQUENTIAL yarn installs — /app root (87s) then /app/backend (30s); wrote 3 .env from user continuation env (/app/backend/.env + /app/.env + /app/frontend/.env), all app URLs → a6e502cd preview URL, preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com), fresh NEXTAUTH_SECRET, GitHub creds (Ov23liBuaGCFqNpp2QzW), user typo EXT_PUBLIC → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY OVERRIDES (backend on LIVE prod Railway PG+Redis): NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false (user env had =true) — verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation ALL skipped. next build standalone OK (rc=0, 104s), public/ 12 files intact after build (shim COPIES public — never symlink), preview URL baked into 13 client JS chunks. Health: /health database=connected redis=connected tatum operational (CLOSED, 40 rates) background_jobs.eligible=false; internal :3300 /health + :8001 /api/ /api/csrf-token + :3000 / = 200; external / /api/ /api/csrf-token /auth/login = 200 (google-login-btn + github-login-btn present), bad-creds POST /api/user/login = 401. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; sshpass missing → SSH tunnel disabled.)
- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 16, re-provisioned 2026-07-10 09:00-09:15 UTC: fresh container, SEQUENTIAL yarn installs /app (81s) then /app/backend (31s), 3 .env files from user continuation env (backend/.env + /app/.env + frontend/.env) — all app URLs → preview URL, preview host FIRST in CORS, fresh NEXTAUTH_SECRET, GitHub creds (Ov23liBuaGCFqNpp2QzW), EXT_PUBLIC typo → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY overrides NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false verified in logs (error-digest / webhook-migration / BullMQ worker / startup-reconciliation all skipped). next build standalone OK (rc=0), public/ 42/42 intact after build (shim COPIES public — never symlink). Health: Railway PG + Redis + Tatum OK (40 rates), internal :8001 /api/ /health /api/csrf-token = 200 + :3300 /health = 200, frontend :3000 / + /auth/login = 200, external preview /api/ /api/csrf-token / = 200, /auth/login has google-login-btn + github-login-btn, bad-creds 401. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko/Tatum fallback; SSH tunnel disabled.)
- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 15, re-provisioned 2026-07-10: fresh container, SEQUENTIAL yarn installs /app (80s) then /app/backend (29s), 3 .env files from user continuation env (backend/.env + /app/.env + frontend/.env) — all app URLs → preview URL, preview host FIRST in CORS, fresh NEXTAUTH_SECRET, GitHub creds already in = form (Ov23liBuaGCFqNpp2QzW), EXT_PUBLIC typo → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true, GOOGLE_CLIENT_KEY kept \\n-escaped, PORT omitted (server.py injects 3300), OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4 included. SAFETY overrides NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false verified in logs (error-digest / webhook-migration / BullMQ worker / startup-reconciliation all skipped). next build standalone OK, public/ 42/42 intact after build (no symlink — shim copies). Health: Railway PG + Redis + Tatum OK (40 rates), internal :8001 /api/ /health /api/csrf-token = 200, frontend :3000 / + /auth/login = 200, external preview /api/ /api/csrf-token / /auth/login = 200 with google-login-btn + github-login-btn, bad-creds 401. Expected quirks: Binance geo-blocked (WS 451) → CoinGecko fallback; sshpass missing → SSH tunnel disabled.)
- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 14, re-provisioned 2026-07-10: fresh container, SEQUENTIAL yarn installs /app then /app/backend, 3 .env files from user continuation env incl. OPENAI_API_KEY + SUPPORT_CHAT_MODEL=gpt-5.4, next build standalone, SAFETY overrides NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false, all health checks green)
- ⚠️⚠️ CRITICAL PROVISIONING RULE (session 14 ROOT CAUSE): NEVER symlink /app/public into .next/standalone. Next.js cleanDistDir (lib/recursive-delete) FOLLOWS directory symlinks — the old `ln -sfn /app/public .next/standalone/public` shim line meant EVERY `next build` deleted all files inside /app/public, and platform auto-commits then recorded the deletion in git (this broke DO deploys 3×: kaniko "lstat /.app_platform_workspace/public: no such file or directory"). The shim start script in /app/frontend/package.json now COPIES public/ (`rm -rf .next/standalone/public && cp -a /app/public/. .next/standalone/public/`). If /app/public is ever empty again, restore with `cp -r /app/assets/public-runtime/. /app/public/` (tracked 42-file mirror; keep in sync when adding runtime assets). The .env FILE symlink and .next/static symlink are safe (file symlinks are only unlinked; static's target lives inside .next anyway).
- DigitalOcean deploy status (2026-07-10 05:30 UTC): deployment b2643132 (commit cd81f6c) ACTIVE since 04:23Z; dynopay.com / + /health + /favicon.ico = 200. The overnight ERROR deploys (02:58–03:32Z) were the public/-missing kaniko error on pre-srcguard commits — resolved by the srcguard Dockerfile commit; future-proofed by the session-14 symlink fix above. User's DO API token was provided in chat (session 14) — NOT stored in repo; advise rotation.
- Session 14 backend additions: support-chat attachments (POST /api/support/chat/upload, multer, 5MB, PNG/JPG/WEBP/GIF/PDF → <uploads>/support-chat served at /api/static/support-chat/), OpenAI vision on image attachments, history/chat return createdAt + attachment fields + replied_at. Live Railway PG got additive columns attachment_url/attachment_name/attachment_type on tbl_support_chat_message (scripts/add_support_chat_attachment_cols.js).
- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 13, re-provisioned 2026-07-10: fresh container, SEQUENTIAL yarn installs /app then /app/backend, 3 .env files from user continuation env incl. OPENAI_API_KEY, next build standalone, SAFETY overrides NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false, all health checks green; public/ restored from git 3567cad2^ after 2nd auto-commit deletion + Dockerfile srcguard hardening + assets/public-runtime mirror)
- (Older preview URL: https://crypto-checkout-39.preview.emergentagent.com — session 12, re-provisioned 2026-07-10: fresh container, yarn install /app + /app/backend, 3 .env files written from user-supplied continuation env, next build standalone, SAFETY overrides NODE_ENV=production / WORKER_ROLE=secondary / ENABLE_BACKGROUND_JOBS=false, all health checks green: Railway PG + Redis + Tatum connected, external /api/ + / + /auth/login = 200, bad creds 401, background_jobs.eligible=false)
- (Older preview URLs: https://crypto-checkout-39.preview.emergentagent.com)
- Re-provisioned 2026-07-10 (session 11) on a fresh container from user-supplied `<continuation_request>` .env: `yarn install` in /app (117s, plain install) + /app/backend (70s); wrote /app/backend/.env, /app/.env, /app/frontend/.env; ran `next build` (standalone output — REQUIRED, frontend shim runs `node .next/standalone/server.js`). All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the 319158e8 preview URL; preview host FIRST in CORS_ALLOWED_ORIGINS (+ dynopay.com + checkout.dynopay.com). Fresh NEXTAUTH_SECRET generated (user env had the literal `openssl rand` command string); GITHUB creds colon-syntax → `=` (Client ID Ov23liBuaGCFqNpp2QzW); EXT_PUBLIC typo → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept `\\n`-escaped; PORT omitted from backend/.env (server.py injects PORT=3300 for the Node process). SAFETY overrides (backend shares LIVE production Railway PG + Redis): NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false — verified in logs ("BACKGROUND JOBS DISABLED"; error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Health verified: Railway PostgreSQL + Redis + Tatum connected (40 rates cached, circuit CLOSED); internal :8001 /api/ /health /api/csrf-token = 200; frontend :3000 / + /auth/login = 200 (Ready in 211ms); external preview /api/ /api/csrf-token / /auth/login = 200 with google-login-btn + github-login-btn present; POST /api/user/login bad creds → 401 "Invalid email or password". Expected env quirks: Binance geo-blocked (WS 451) → CoinGecko fallback; sshpass missing → SSH tunnel manager disabled.
- PRODUCTION DEPLOYMENT = DIGITALOCEAN App Platform (NOT Railway — Dockerfile.frontend/railway-frontend.json are legacy leftovers). App "dynopay" id f86b27dc-feb0-4a44-a4e9-ebd2053e0468, service "dynoredesign", live https://dynopay.com (default ingress https://dynopay-bcibf.ondigitalocean.app), region ams, repo databasedyno/DynoRedesign branch New-Onboarding2, autodeploy on push. Single container from repo-root /Dockerfile: start-all.sh runs nginx(:8001, public) + Express backend(:3300, node dist/server.js) + Next.js STANDALONE server(:3000, node server.js) — so `output: "standalone"` in next.config.mjs is REQUIRED by prod, never remove. Prod env (app-level): WORKER_ROLE=primary + ENABLE_BACKGROUND_JOBS=true on BOTH of 2 instances (flagged to user: background jobs run twice in parallel).
- 2026-07-09 (session 10, user request): (1) start-all.sh frontend line now has NODE_OPTIONS="--no-warnings --max-old-space-size=1536" — takes effect on next "Save to GitHub" push→autodeploy; (2) instance upgraded apps-s-1vcpu-1gb → apps-s-1vcpu-2gb (×2, $12→$25/mo each) via DO API spec PUT; deployment f4e35db6 went ACTIVE, dynopay.com / + /health = 200. User's DO API token was provided in chat (session 10) — NOT stored in repo; advise rotation.
- FRONTEND RUN MODE (changed 2026-07-09, session 10, per user request): supervisor's `yarn start` (in /app/frontend) now execs the STANDALONE server — `node /app/.next/standalone/server.js` with `NODE_OPTIONS=--max-old-space-size=1536 HOSTNAME=0.0.0.0 PORT=3000` — instead of `next start`. The shim start script first re-creates symlinks (survive rebuilds): .next/standalone/.next/static → /app/.next/static, .next/standalone/public → /app/public, .next/standalone/.env → /app/.env (standalone loads env from ITS OWN dir, not /app). `output: "standalone"` KEPT in next.config.mjs (prod depends on it). After any frontend code change: run `cd /app && node_modules/.bin/next build` then `sudo supervisorctl restart frontend`. Supervisor conf itself is READONLY — both requested changes implemented via /app/frontend/package.json. Verified: node child has NODE_OPTIONS applied; local+external / /auth/login = 200 (SSO buttons present); /_next/static + public assets = 200; local /api/auth/providers = 200 with preview NEXTAUTH_URL (proves .env symlink works).
- Re-provisioned 2026-07-09 (session 10) on a fresh container from user-supplied `<continuation_request>` .env: `yarn install` in /app (111s, plain install) + /app/backend (72s); wrote /app/backend/.env, /app/.env, /app/frontend/.env; ran `next build` (frontend shim uses `next start`, so a production build is REQUIRED — frontend is FATAL without it). All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the c1d37d98 preview URL; preview host FIRST in CORS_ALLOWED_ORIGINS (+ crypto-gateway-24 alias kept). Fresh NEXTAUTH_SECRET generated (user env had the literal `openssl rand` command string); GITHUB creds colon-syntax → `=` (Client ID Ov23liBuaGCFqNpp2QzW); EXT_PUBLIC typo → NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true; GOOGLE_CLIENT_KEY kept `\\n`-escaped; PORT omitted from backend/.env (server.py injects PORT=3300 for the Node process). SAFETY overrides (backend shares LIVE production Railway PG + Redis): NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false — verified in logs (error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Health verified: Railway PostgreSQL + Redis + Tatum connected (40 rates cached); internal :8001 /api/ /health /api/csrf-token = 200; frontend :3000 = 200; external preview /api/ /api/csrf-token / /auth/login = 200 with google-login-btn + github-login-btn present; POST /api/user/login bad creds → 401. Expected env quirks: Binance geo-blocked → CoinGecko fallback; sshpass missing → SSH tunnel manager disabled.
- Re-provisioned 2026-07-09 (session 9) on a fresh container from user-supplied `<continuation_request>` .env: `yarn install` in /app (plain install) + /app/backend; wrote /app/backend/.env, /app/.env, /app/frontend/.env. All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the 767dbee0 preview URL; preview host FIRST in CORS_ALLOWED_ORIGINS (+ transaction-platform-3 alias kept); NEXTAUTH_SECRET freshly generated (user env had the literal `openssl rand` command string); GOOGLE_CLIENT_KEY kept with `\\n` escaping (normalizePrivateKey handles it); GITHUB_CLIENT_ID/SECRET converted from colon-separator to `=` (Client ID Ov23liBuaGCFqNpp2QzW); user typo `EXT_PUBLIC_ENABLE_GITHUB_AUTH` corrected to NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true. SAFETY overrides re-applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false (user env said true — deliberately overridden; verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Binance geo-blocked → CoinGecko fallback (expected); sshpass missing → SSH tunnel manager disabled (expected). Health verified: Railway PostgreSQL + Redis + Tatum connected, internal :8001 `/api/ /health /api/csrf-token` = 200, frontend :3000 = 200, external preview `/api/ /api/csrf-token / /auth/login` = 200; /auth/login HTML contains google-login-btn + github-login-btn; POST /api/user/login bad creds → proper 401.
- Re-provisioned 2026-07-09 (session 8) on a fresh container from user-supplied `<continuation_request>` .env: `yarn install` in /app (Next.js 14.2.35, plain install) + /app/backend (Node/TS); wrote /app/backend/.env, /app/.env, /app/frontend/.env. All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the daa5e6f5 preview URL; preview host FIRST in CORS_ALLOWED_ORIGINS; NEXTAUTH_SECRET freshly generated (user env had the literal `openssl rand` command string); GOOGLE_CLIENT_KEY kept with `\\n` escaping (normalizePrivateKey handles it); GITHUB_CLIENT_ID/SECRET converted from colon-separator to `=` — NOTE: user supplied a NEW GitHub OAuth Client ID this session: Ov23liBuaGCFqNpp2QzW (older sessions used Ov23liyOOHYelH9Y6Vp0); user typo `EXT_PUBLIC_ENABLE_GITHUB_AUTH` corrected to NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true. SAFETY overrides re-applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false (user env said true — deliberately overridden; verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Binance geo-blocked → CoinGecko fallback (expected); sshpass missing → SSH tunnel manager disabled (expected). Health verified: Railway PostgreSQL + Redis + Tatum connected (40 rates cached), internal :8001 `/api/ /health /api/csrf-token` = 200, frontend :3000 = 200, external preview `/api/ /api/csrf-token / /auth/login` = 200 (first external checks 503'd for ~30s — ingress warm-up, resolved by itself); /auth/login HTML contains google-login-btn + github-login-btn; POST /api/user/login bad creds → proper 401.
- Re-provisioned 2026-07 (session 7) on a fresh container from user-supplied `<continuation_request>` Railway .env: `yarn install` in /app (Next.js — plain install, `--frozen-lockfile` fails: lockfile out of sync) + /app/backend (Node/TS, 34s); wrote /app/backend/.env, /app/.env, /app/frontend/.env. All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the 090222ef preview URL; preview host FIRST in CORS_ALLOWED_ORIGINS; NEXTAUTH_SECRET freshly generated (user env had the literal `openssl rand` command string); GOOGLE_CLIENT_KEY kept with `\\n` escaping (normalizePrivateKey handles it); GITHUB_CLIENT_ID/SECRET converted from colon-separator to `=`; user typo `EXT_PUBLIC_ENABLE_GITHUB_AUTH` corrected to NEXT_PUBLIC_ENABLE_GITHUB_AUTH=true. SAFETY overrides re-applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false (user env said true — deliberately overridden; verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Binance geo-blocked → CoinGecko fallback (expected); sshpass missing → SSH tunnel manager disabled (expected). Health verified: Railway PostgreSQL + Redis + Tatum connected (40 rates cached), internal :8001 `/api/ /health /api/csrf-token` = 200, frontend :3000 = 200, external preview `/api/ /api/csrf-token / /auth/login` = 200; /auth/login HTML contains google-login-btn + github-login-btn; POST /api/user/login bad creds → proper 401.
- Re-provisioned 2026-07-08 (session 6) on a fresh container (job ad7ecb61) from user-supplied `<continuation_request>` Railway .env: `yarn install` in /app (Next.js 14.2.35) + /app/backend (Node/TS); wrote /app/backend/.env, /app/.env, /app/frontend/.env. All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the 3aa3b1bf preview URL; preview host first in CORS_ALLOWED_ORIGINS; NEXTAUTH_SECRET freshly generated (user env had the literal `openssl rand` command string); GOOGLE_CLIENT_KEY kept with `\\n` escaping (normalizePrivateKey handles it); GITHUB_CLIENT_ID/SECRET converted from colon-separator to `=`. SAFETY overrides re-applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false (user env said true — deliberately overridden; verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Binance geo-blocked → CoinGecko fallback (expected). Health verified: Railway PostgreSQL + Redis + Tatum-API connected, internal :3300 & :8001 `/health /api/csrf-token` = 200, frontend :3000 = 200, external preview `/ /api/ /api/csrf-token /auth/login` = 200; POST /api/user/login returns proper 401 for bad creds; /auth/login HTML shows Google + GitHub SSO buttons.
- Re-provisioned 2026-07-08 (session 5) on a fresh container from user-supplied `<continuation_request>` .env: `yarn install` in /app (Next.js) + /app/backend (Node/TS); wrote /app/backend/.env, /app/.env, /app/frontend/.env. All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the a12ec985 preview URL; preview host first in CORS_ALLOWED_ORIGINS; NEXTAUTH_SECRET freshly generated (user env had the literal command string); GOOGLE_CLIENT_KEY kept with `\\n` escaping (normalizePrivateKey handles it). SAFETY overrides re-applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false (user env said true — deliberately overridden; verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Binance geo-blocked → CoinGecko fallback (expected). Health verified: Railway PostgreSQL + Redis connected, internal :8001 `/api/ /health /api/csrf-token` = 200, frontend :3000 = 200, external preview `/api/ /api/csrf-token /api/geo-detect / /auth/login` = 200.
- Re-provisioned 2026-07-08 (session 4) on a fresh container from user-supplied `<continuation_request>` credentials: `yarn install` in /app (Next.js 14.2.35) + /app/backend (Node/TS); wrote /app/backend/.env, /app/.env, /app/frontend/.env. All app URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) → the 53e54123 preview URL; preview host added to CORS_ALLOWED_ORIGINS; NEXTAUTH_SECRET freshly generated; GOOGLE_CLIENT_KEY kept with `\\n` escaping (normalizePrivateKey handles it). SAFETY overrides re-applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false (user env said primary/true — deliberately overridden; verified in logs: error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped). Health verified: PostgreSQL + Redis connected, internal :8001 `/api/ /health /api/csrf-token` = 200, frontend :3000 = 200, external preview `/api/ / /auth/login` = 200.
- Frontend .env: /app/.env (NEXT_PUBLIC_BASE_URL + NEXT_PUBLIC_SERVER_URL + NEXTAUTH_URL point to the preview URL; /api/* routed to backend on port 8001). NOTE: no /app/.env.local present — Next.js reads /app/.env.
- Backend .env: /app/backend/.env (points to Railway PostgreSQL `railway` DB on `roundhouse.proxy.rlwy.net:23599`)
- Redis: nozomi.proxy.rlwy.net:15794
- Backend runs NODE_ENV=production (sequelize .sync({}) = no destructive ALTER) + WORKER_ROLE=secondary + ENABLE_BACKGROUND_JOBS=false (API-only; ALL cron jobs/sweeps/conversions/webhook-migration/emails DISABLED — safe alongside production).
- CORS_ALLOWED_ORIGINS includes the preview URL + dynopay.com + checkout.dynopay.com.
- Re-provisioned 2026-07-07 (session 3) on a fresh container using user-supplied credentials from `<continuation_request>`: `yarn install` in /app (Next.js 14.2.35) and /app/backend (Node/TS); wrote /app/backend/.env, /app/.env, /app/frontend/.env. App URLs (SERVER_URL / FRONTEND_URL / CHECKOUT_URL / NEXTAUTH_URL / NEXT_PUBLIC_BASE_URL / NEXT_PUBLIC_SERVER_URL / REACT_APP_BACKEND_URL) all pointed to `https://crypto-checkout-39.preview.emergentagent.com`. Preview host added to CORS_ALLOWED_ORIGINS. NEXTAUTH_SECRET regenerated (user provided the literal shell command). GOOGLE_CLIENT_KEY preserved with literal `\n` escaping. SAFETY overrides applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false — re-verified in logs (error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped; only the read-only rate-cache refresh + Binance geo-block detection run). Binance WS geo-blocked → CoinGecko REST fallback active (expected). Health verified: PostgreSQL + Redis connected, internal `/api/ /health /api/csrf-token /api/geo-detect /auth/login` = 200, external preview `/api/ /auth/login` = 200. Emergent LLM key added: `sk-emergent-eAe4bE61d91BbFcA6C` (only used by offline SEO generator, not the running app).
- Re-provisioned 2026-07-07 (session 2) from a fresh checkout using user-supplied credentials: `yarn install` in /app and /app/backend; recreated /app/.env (frontend app-URLs → https://crypto-checkout-39.preview.emergentagent.com for NEXT_PUBLIC_BASE_URL/NEXT_PUBLIC_SERVER_URL/SERVER_URL/FRONTEND_URL/NEXTAUTH_URL + a freshly generated NEXTAUTH_SECRET) and /app/backend/.env (Railway PG + Redis + all API keys). SAFETY overrides applied: NODE_ENV=production, WORKER_ROLE=secondary, ENABLE_BACKGROUND_JOBS=false — re-verified in logs (error-digest / webhook-URL-migration / BullMQ webhook worker / startup-reconciliation all skipped; only the read-only rate-cache refresh runs). CORS_ALLOWED_ORIGINS = a407732e-… preview host + dynopay.com + checkout.dynopay.com. Verified: PostgreSQL + Redis connected, /health + /api/csrf-token = 200 (internal + external), homepage SSR returns 200.

## Primary QA Account (active user with $18k+ in transactions)
- Email: hostbay@moxx.co
- Password: Katiekendra123@
- Notes: Main account for QA testing. Has significant transaction history (~$18k lifetime volume).

## IMPORTANT — Preview backend DB note
- The preview backend's Railway PostgreSQL appears to have test users only.
- The previously documented admin `moxxcompany@gmail.com` does NOT exist in this DB.

## Throwaway QA Merchant (verified email)
- Email: qa.onboard.1782585233@dynopaytest.com
- Password: QaOnboard#2026
- user_id: 3 (email_verified=true). Has 1 company: "QA Test Co" (company_id=2). No wallet, no payment link.
- has_password: true

## Empty/new QA Merchant (verified email) — for dashboard empty-state testing
- Email: qa.empty.1782626169@dynopaytest.com
- Password: QaEmpty#2026
- user_id: 8 (email_verified=true). NO company, NO wallet, NO payment link, NO transactions.
- has_password: true

## i18n / language switching (IMPORTANT)
- The client language is stored in localStorage under the key **`lang`** (values: `en`/`pt`/`es`/`fr`/`de`/`nl`), NOT `i18nextLng`. Setting `i18nextLng` has NO effect.
- To force a language in a test: `localStorage.setItem('lang','de'); localStorage.setItem('lang_manual','true');` then **reload** (SSR always renders English first, the client switches on hydration ~1-2s).
- Easiest in a real browser: use the header language dropdown (e.g. pick "DE - Deutsch").

## Mint a 30-day JWT (bypass OTP) for logged-in UI tests
- Run: `node /app/scripts/mint_ux_tokens.js` — prints JWTs for `hostbay@moxx.co` (data-rich), `qa.empty...`, `qa.onboard...`.
- Inject: on the app origin `localStorage.setItem('token','<JWT>')`, then navigate to `/dashboard` (or `/create-pay-link`, `/wallet`, etc.).
- Use `hostbay@moxx.co` for pay-link tests — it has a company + wallet, so `/create-pay-link` reaches the Payment Settings form (the empty QA merchant is blocked by the onboarding gate).

## How to test logged-in flows (login is OTP-gated)
1. **Token injection (recommended for frontend tests)**: obtain a valid 30-day JWT and inject it:
   - On the app origin: `localStorage.setItem('token', '<JWT>')` then navigate to `/dashboard`.
2. **Scripted login via Redis OTP**: after `POST /api/user/login`, read the OTP with
   Redis key `login_otp:<session>:json` (via ioredis at redis://default:HAEMJseUAdqAjpiICURxlefSoSYXKEUg@nozomi.proxy.rlwy.net:15794),
   then `POST /api/user/verifyLoginOTP`.

## Creating a fresh verified test merchant
1. `POST /api/user/registerUser` {name,email,password} → returns `accessToken` + `userData.user_id`.
2. Read OTP from Redis key `email-verify:<user_id>:json`.
3. `POST /api/user/verify-email` {otp} (Bearer accessToken) → email_verified=true.
Header required: `User-Agent: Mozilla/5.0 ... Chrome/120 Safari/537.36`

## Notes
- Admin passwords use bcrypt (12 rounds) with transparent SHA-256 migration on first login.
- JWT access tokens expire after 30 days.
- OTP rate limiter: 10 requests per IP:contact combo per 15 minutes.
- Login activity is recorded in `tbl_login_activities` on every successful login.

## GitHub OAuth (added 2026-07-08, session 5)
- OAuth App Client ID: Ov23liyOOHYelH9Y6Vp0 (secret in /app/backend/.env GITHUB_CLIENT_SECRET)
- Registered callback: https://dynopay.com/auth/github/callback (PROD ONLY — preview end-to-end will fail redirect_uri validation unless a 2nd dev OAuth app is created with the preview callback)
- Flow: login/register GitHub icon button → github.com authorize → /auth/github/callback → POST /api/user/github-signin {code, redirectUri} (CSRF-protected)
- login_type enum now includes 'GITHUB'; GitHub identity stored as external_id='github:<id>'

## QA fixture (added 2026-07-08, session 5c)
- tbl_user_wallet wallet_id=15: user_id=3 (qa.onboard), company_id=2, BTC, address 1JH5TnZzjYTf1yYwBDLjWoHgkAcCHc1Do7, name "QA BTC Wallet".
- Purpose: qa.onboard has exactly ONE wallet so the /wallet Add dialog shows 1 disabled "Added" + 14 enabled currencies, and the BTC card enables edit-dialog testing. hostbay has ALL 15 currencies → its Add Wallet button is hidden by design.
